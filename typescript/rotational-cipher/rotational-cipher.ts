export default class RotationalCipher {
  alphabet = {
      'a': 1,
      'b': 2,
      'c': 3,
      'd': 4,

      

  }
  
    static rotate(message:string, degree:number): string {
    return "";
  }
}
